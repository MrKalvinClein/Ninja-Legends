local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Ninja Legends",
    Icon = "sword",
    Author = "Script Hub",
    HideSearchBar = false,
    Transparency = 0.4
})

local FarmingTab = Window:Tab({
    Title = "Farming",
})

FarmingTab:Section({
    Title = "Karma Farms",
})

local GKEnabled = false
local BKEnabled = false

FarmingTab:Toggle({
    Title = "Auto-Good Karma",
    Callback = function(state)
        GKEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-Bad Karma",
    Callback = function(state)
        BKEnabled = state
    end
})

FarmingTab:Section({
    Title = "Ultra Coins",
})

local SwingEnabled = false
local SellEnabled = false
local FullSellEnabled = false

FarmingTab:Toggle({
    Title = "Auto-Swing",
    Callback = function(state)
        SwingEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-Sell",
    Callback = function(state)
        SellEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-Full Sell",
    Callback = function(state)
        FullSellEnabled = state
    end
})

FarmingTab:Section({
    Title = "Ultra Chi",
})

local ChiEnabled = false

FarmingTab:Toggle({
    Title = "Auto-Chi",
    Callback = function(state)
        ChiEnabled = state
    end
})

FarmingTab:Section({
    Title = "Boss Farms",
})

local BossEnabled = false
local EBossEnabled = false
local ABossEnabled = false
local SBossEnabled = false
local AllBossesEnabled = false

FarmingTab:Toggle({
    Title = "Auto-Robot Boss",
    Callback = function(state)
        BossEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-Eternal Boss",
    Callback = function(state)
        EBossEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-Ancient Boss",
    Callback = function(state)
        ABossEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-Santa Boss",
    Callback = function(state)
        SBossEnabled = state
    end
})

FarmingTab:Toggle({
    Title = "Auto-All Bosses",
    Callback = function(state)
        AllBossesEnabled = state
    end
})

FarmingTab:Section({
    Title = "Give Pet Levels",
})

local PetLevelsEnabled = false

FarmingTab:Toggle({
    Title = "Auto-Pet Levels",
    Callback = function(state)
        PetLevelsEnabled = state
    end
})

local AutoBuyTab = Window:Tab({
    Title = "Auto-Buy",
})

AutoBuyTab:Section({
    Title = "Auto-Buy Stuff",
})

local RankEnabled = false
local SwordEnabled = false
local BeltEnabled = false
local SkillEnabled = false
local ShurikensEnabled = false

AutoBuyTab:Toggle({
    Title = "Auto-Rank",
    Callback = function(state)
        RankEnabled = state
    end
})

AutoBuyTab:Toggle({
    Title = "Auto-Sword",
    Callback = function(state)
        SwordEnabled = state
    end
})

AutoBuyTab:Toggle({
    Title = "Auto-Belt",
    Callback = function(state)
        BeltEnabled = state
    end
})

AutoBuyTab:Toggle({
    Title = "Auto-Skills",
    Callback = function(state)
        SkillEnabled = state
    end
})

AutoBuyTab:Toggle({
    Title = "Auto-Shurikens",
    Callback = function(state)
        ShurikensEnabled = state
    end
})

local PetsTab = Window:Tab({
    Title = "Pet Stuff",
})

PetsTab:Section({
    Title = "Open Pets",
})

local Crystals = {}
for i,v in next, Workspace.mapCrystalsFolder:GetChildren() do 
    if v then 
        table.insert(Crystals, v.Name)
    end
end

local selectedCrystal = Crystals[1] or ""
local TEggEnabled = false

PetsTab:Dropdown({
    Title = "Crystals",
    Values = Crystals,
    Callback = function(option)
        selectedCrystal = option
    end
})

PetsTab:Toggle({
    Title = "Open Eggs",
    Callback = function(state)
        TEggEnabled = state
    end
})

PetsTab:Section({
    Title = "Pet Options",
})

local EvolveEnabled = false
local EternaliseEnabled = false
local ImmortalizePet = false

PetsTab:Toggle({
    Title = "Auto-Evolve",
    Callback = function(state)
        EvolveEnabled = state
    end
})

PetsTab:Toggle({
    Title = "Auto-Eternalise",
    Callback = function(state)
        EternaliseEnabled = state
    end
})

PetsTab:Toggle({
    Title = "Auto-Immortalize",
    Callback = function(state)
        ImmortalizePet = state
    end
})

PetsTab:Section({
    Title = "Sell Pets",
})

local SBasic = false
local SAdvanced = false
local SRare = false
local SEpic = false
local SUnique = false
local SOmega = false
local SElite = false
local SInfinity = false

PetsTab:Toggle({
    Title = "Sell All Basic",
    Callback = function(state)
        SBasic = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Advanced",
    Callback = function(state)
        SAdvanced = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Rare",
    Callback = function(state)
        SRare = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Epic",
    Callback = function(state)
        SEpic = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Unique",
    Callback = function(state)
        SUnique = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Omega",
    Callback = function(state)
        SOmega = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Elite",
    Callback = function(state)
        SElite = state
    end
})

PetsTab:Toggle({
    Title = "Sell All Infinity",
    Callback = function(state)
        SInfinity = state
    end
})

local Pets2Tab = Window:Tab({
    Title = "More Pet Stuff",
})

Pets2Tab:Section({
    Title = "Sell Separate Pets",
})

local S1 = false
local S2 = false
local S3 = false
local S4 = false
local S5 = false

Pets2Tab:Toggle({
    Title = "Sell All Winter Kitty",
    Callback = function(state)
        S1 = state
    end
})

Pets2Tab:Toggle({
    Title = "Sell All Polar Bear",
    Callback = function(state)
        S2 = state
    end
})

Pets2Tab:Toggle({
    Title = "Sell All Sensei Reindeer",
    Callback = function(state)
        S3 = state
    end
})

Pets2Tab:Toggle({
    Title = "Sell All Dark Penguin",
    Callback = function(state)
        S4 = state
    end
})

Pets2Tab:Toggle({
    Title = "Sell All Sleigh Rider",
    Callback = function(state)
        S5 = state
    end
})

local MiscTab = Window:Tab({
    Title = "Misc",
})

MiscTab:Section({
    Title = "Other OP Scripts",
})

local FastShuriken = false
local SlowShuriken = false
local InvisEnabled = false

MiscTab:Toggle({
    Title = "Fast Shuriken",
    Callback = function(state)
        FastShuriken = state
    end
})

MiscTab:Toggle({
    Title = "Slow Shuriken",
    Callback = function(state)
        SlowShuriken = state
    end
})

MiscTab:Toggle({
    Title = "Invisibility",
    Callback = function(state)
        InvisEnabled = state
    end
})

MiscTab:Space()

MiscTab:Button({
    Title = "Collect All Chest",
    Callback = function()
        Workspace.mythicalChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.goldenChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.enchantedChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.magmaChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.legendsChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.eternalChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.saharaChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.thunderChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.ancientChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.midnightShadowChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.groupRewardsCircle["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace["Daily Chest"].circleInner.CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace["wonderChest"].circleInner.CFrame = player.Character.HumanoidRootPart.CFrame
        wait(3.5)
        Workspace.wonderChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        Workspace.midnightShadowChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        Workspace.ancientChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.midnightShadowChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.thunderChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.saharaChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.eternalChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.legendsChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.magmaChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.enchantedChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.goldenChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.mythicalChest["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace.groupRewardsCircle["circleInner"].CFrame = Workspace.Part.CFrame
        Workspace["Daily Chest"].circleInner.CFrame = Workspace.Part.CFrame
    end
})

MiscTab:Button({
    Title = "Collect Light Chest",
    Callback = function()
        Workspace.lightKarmaChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(5)
        Workspace.lightKarmaChest["circleInner"].CFrame = Workspace.Part.CFrame
    end
})

MiscTab:Button({
    Title = "Collect Evil Chest",
    Callback = function()
        Workspace.evilKarmaChest["circleInner"].CFrame = player.Character.HumanoidRootPart.CFrame
        wait(5)
        Workspace.evilKarmaChest["circleInner"].CFrame = Workspace.Part.CFrame
    end
})

MiscTab:Button({
    Title = "Unlock Islands",
    Callback = function()
        for i,v in next, Workspace.islandUnlockParts:GetChildren() do 
            if v then 
                player.Character.HumanoidRootPart.CFrame = v.islandSignPart.CFrame
                wait(.5)
            end
        end
    end
})

MiscTab:Button({
    Title = "Max Jumps",
    Callback = function()
        while wait(.0001) do
            player.multiJumpCount.Value = "50"
        end
    end
})

MiscTab:Button({
    Title = "Hide Name",
    Callback = function()
        local plrname = player.Name
        Workspace[plrname].Head.nameGui:Destroy()
    end
})

MiscTab:Button({
    Title = "ESP",
    Callback = function()
        function isnil(thing)
            return (thing == nil)
        end
        local function round(n)
            return math.floor(tonumber(n) + 0.5)
        end
        function UpdatePlayerChams()
            for i,v in pairs(Players:GetChildren()) do
                pcall(function()
                    if not isnil(v.Character) then
                        for _,k in pairs(v.Character:GetChildren()) do
                            if k:IsA'BasePart' and not k:FindFirstChild'Cham' then
                                local cham = Instance.new('BoxHandleAdornment',k)
                                cham.ZIndex= 10
                                cham.Adornee=k
                                cham.AlwaysOnTop=true
                                cham.Size=k.Size
                                cham.Transparency=.8
                                cham.Color3=Color3.new(0,0,1)
                                cham.Name = 'Cham'
                            end
                        end
                        if not isnil(v.Character.Head) and not v.Character.Head:FindFirstChild'NameEsp' then
                            local bill = Instance.new('BillboardGui',v.Character.Head)
                            bill.Name = 'NameEsp'
                            bill.Size=UDim2.new(1,200,1,30)
                            bill.Adornee=v.Character.Head
                            bill.AlwaysOnTop=true
                            local name = Instance.new('TextLabel',bill)
                            name.TextWrapped=true
                            name.Text = (v.Name ..' '.. round((player.Character.Head.Position - v.Character.Head.Position).Magnitude/3) ..'m')
                            name.Size = UDim2.new(1,0,1,0)
                            name.TextYAlignment='Top'
                            name.TextColor3=Color3.new(1,1,1)
                            name.BackgroundTransparency=1
                        else
                            v.Character.Head.NameEsp.TextLabel.Text = (v.Name ..' '.. round((player.Character.Head.Position - v.Character.Head.Position).Magnitude/3) ..'m')
                        end
                    end
                end)
            end
        end
        while wait() do
            UpdatePlayerChams()
        end
    end
})

MiscTab:Button({
    Title = "Destroy GUI",
    Callback = function()
        Window:Destroy()
    end
})

local TeleportsTab = Window:Tab({
    Title = "Teleports",
})

TeleportsTab:Section({
    Title = "Islands",
})

local Islands = {}
for i,v in next, Workspace.islandUnlockParts:GetChildren() do 
    if v then 
        table.insert(Islands, v.Name)
    end
end

TeleportsTab:Dropdown({
    Title = "Teleports",
    Values = Islands,
    Callback = function(option)
        player.Character.HumanoidRootPart.CFrame = Workspace.islandUnlockParts[option].islandSignPart.CFrame
    end
})

TeleportsTab:Section({
    Title = "Utilitys",
})

TeleportsTab:Button({
    Title = "Shop",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = Workspace.shopAreaCircles["shopAreaCircle11"].circleInner.CFrame
    end
})

TeleportsTab:Button({
    Title = "Skills Shop",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = Workspace.skillAreaCircles["skillsAreaCircle11"].circleInner.CFrame
    end
})

TeleportsTab:Button({
    Title = "Light Skills Shop",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(-116.49514, 3.24800324, 0.0838552266)
    end
})

TeleportsTab:Button({
    Title = "Dark Skills Shop",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(-116.549767, 3.24800324, 58.087841)
    end
})

TeleportsTab:Button({
    Title = "KOTH",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = Workspace.kingOfTheHillPart.CFrame
    end
})

TeleportsTab:Section({
    Title = "Training Areas",
})

TeleportsTab:Button({
    Title = "Mystical Waters (Good)",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(347.74881, 8824.53809, 114.271019)
    end
})

TeleportsTab:Button({
    Title = "Sword of Legends (Good)",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(1834.15967, 38.704483, -141.375641)
    end
})

TeleportsTab:Button({
    Title = "Elemental Tornado (Good)",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(299.758484, 30383.0957, -90.1542206)
    end
})

TeleportsTab:Button({
    Title = "Lava Pit (Bad)",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(-116.631485, 12952.5381, 271.14624)
    end
})

TeleportsTab:Button({
    Title = "Tornado (Bad)",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(325.641174, 16872.0938, -9.9906435)
    end
})

TeleportsTab:Button({
    Title = "Swords Of Ancients (Bad)",
    Callback = function()
        player.Character.HumanoidRootPart.CFrame = CFrame.new(648.365662, 38.704483, 2409.72266)
    end
})

spawn(function()
    while wait(.01) do
        if TEggEnabled then
            local oh1 = "openCrystal"
            local oh2 = selectedCrystal
            ReplicatedStorage.rEvents.openCrystalRemote:InvokeServer(oh1, oh2)
        end
    end
end)

spawn(function()
    while wait() do
        if SwingEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                if player.Character:FindFirstChildOfClass("Tool") then 
                    player.ninjaEvent:FireServer("swingKatana")
                else
                    for i,v in pairs(player.Backpack:GetChildren()) do 
                        if v.ClassName == "Tool" and v:FindFirstChild("attackShurikenScript") then 
                            player.Character.Humanoid:EquipTool(v)
                            wait()
                            if v.ClassName == "Tool" and v:FindFirstChild("attackKatanaScript") then 
                                player.Character.Humanoid:EquipTool(v) 
                            end
                        end
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.01) do
        if SellEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                Workspace.sellAreaCircles["sellAreaCircle7"].circleInner.CFrame = player.Character:WaitForChild("HumanoidRootPart").CFrame
                wait(.1)
                Workspace.sellAreaCircles["sellAreaCircle7"].circleInner.CFrame = Workspace.Part.CFrame
            end
        end
    end
end)

spawn(function()
    while wait(0.01) do
        if FullSellEnabled then 
            if player.Character:WaitForChild("HumanoidRootPart") then
                if player.PlayerGui.gameGui.maxNinjitsuMenu.Visible == true then
                    Workspace.sellAreaCircles["sellAreaCircle7"].circleInner.CFrame = player.Character:WaitForChild("HumanoidRootPart").CFrame
                    wait(.05)
                    Workspace.sellAreaCircles["sellAreaCircle7"].circleInner.CFrame = Workspace.Part.CFrame
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.001) do
        if InvisEnabled then
            local A_1 = "goInvisible"
            local Event = player.ninjaEvent
            Event:FireServer(A_1)
        end
    end
end)

spawn(function()
    while wait(0.00011) do
        if PetLevelsEnabled then
            for _,v in pairs(Workspace.Hoops:GetDescendants()) do
                if v.ClassName == "MeshPart" then
                    v.touchPart.CFrame = player.Character.HumanoidRootPart.CFrame
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.4) do
        if GKEnabled then
            loadstring(game:HttpGet(('https://pastebin.com/raw/AaqHqPyw'),true))()
        end
    end
end)

spawn(function()
    while wait(0.4) do
        if BKEnabled then
            loadstring(game:HttpGet(('https://pastebin.com/raw/wEEB3nQt'),true))()
        end
    end
end)

spawn(function()
    while wait(.001) do
        if BossEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                if Workspace.bossFolder:WaitForChild("RobotBoss"):WaitForChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder.RobotBoss.HumanoidRootPart.CFrame
                    if player.Character:FindFirstChildOfClass("Tool") then
                        player.Character:FindFirstChildOfClass("Tool"):Activate()
                    else
                        for i,v in pairs(player.Backpack:GetChildren()) do
                            if v.ClassName == "Tool" and v:FindFirstChild("attackKatanaScript") then
                                v.attackTime.Value = 0.2
                                player.Character.Humanoid:EquipTool(v)
                            end
                        end
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(.001) do
        if EBossEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                if Workspace.bossFolder:WaitForChild("EternalBoss"):WaitForChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder.EternalBoss.HumanoidRootPart.CFrame
                    if player.Character:FindFirstChildOfClass("Tool") then
                        player.Character:FindFirstChildOfClass("Tool"):Activate()
                    else
                        for i,v in pairs(player.Backpack:GetChildren()) do
                            if v.ClassName == "Tool" and v:FindFirstChild("attackKatanaScript") then
                                v.attackTime.Value = 0.2
                                player.Character.Humanoid:EquipTool(v)
                            end
                        end
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(.001) do
        if ABossEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                if Workspace.bossFolder:WaitForChild("AncientMagmaBoss"):WaitForChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder.AncientMagmaBoss.HumanoidRootPart.CFrame
                    if player.Character:FindFirstChildOfClass("Tool") then
                        player.Character:FindFirstChildOfClass("Tool"):Activate()
                    else
                        for i,v in pairs(player.Backpack:GetChildren()) do
                            if v.ClassName == "Tool" and v:FindFirstChild("attackKatanaScript") then
                                v.attackTime.Value = 0.2
                                player.Character.Humanoid:EquipTool(v)
                            end
                        end
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(.001) do
        if SBossEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                if Workspace.bossFolder:WaitForChild("Samurai Santa"):WaitForChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder["Samurai Santa"].HumanoidRootPart.CFrame
                    if player.Character:FindFirstChildOfClass("Tool") then
                        player.Character:FindFirstChildOfClass("Tool"):Activate()
                    else
                        for i,v in pairs(player.Backpack:GetChildren()) do
                            if v.ClassName == "Tool" and v:FindFirstChild("attackKatanaScript") then
                                v.attackTime.Value = 0.2
                                player.Character.Humanoid:EquipTool(v)
                            end
                        end
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(.001) do
        if AllBossesEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                if Workspace.bossFolder:FindFirstChild("Samurai Santa") then
                    player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder["Samurai Santa"].HumanoidRootPart.CFrame
                else
                    if not Workspace.bossFolder:FindFirstChild("Samurai Santa") then
                        if Workspace.bossFolder:FindFirstChild("AncientMagmaBoss") then
                            player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder.AncientMagmaBoss.HumanoidRootPart.CFrame
                        else
                            if not Workspace.bossFolder:FindFirstChild("AncientMagmaBoss") then
                                if Workspace.bossFolder:FindFirstChild("EternalBoss") then
                                    player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder.EternalBoss.HumanoidRootPart.CFrame
                                else
                                    if not Workspace.bossFolder:FindFirstChild("EternalBoss") then
                                        if Workspace.bossFolder:FindFirstChild("RobotBoss") then
                                            player.Character.HumanoidRootPart.CFrame = Workspace.bossFolder.RobotBoss.HumanoidRootPart.CFrame
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
                if player.Character:FindFirstChildOfClass("Tool") then
                    player.Character:FindFirstChildOfClass("Tool"):Activate()
                else
                    for i,v in pairs(player.Backpack:GetChildren()) do
                        if v.ClassName == "Tool" and v:FindFirstChild("attackKatanaScript") then
                            v.attackTime.Value = 0.2
                            player.Character.Humanoid:EquipTool(v)
                        end
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.5) do
        if SwordEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local oh1 = "buyAllSwords"
                local oh2 = {"Ground", "Astral Island", "Space Island","Tundra Island", "Eternal Island", "Sandstorm", "Thunderstorm", "Ancient Inferno Island", "Midnight Shadow Island", "Mythical Souls Island", "Winter Wonder Island"}
                for i = 1,#oh2 do
                    player.ninjaEvent:FireServer(oh1, oh2[i])
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.5) do
        if BeltEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local oh1 = "buyAllBelts"
                local oh2 = {"Ground", "Astral Island", "Space Island","Tundra Island", "Eternal Island", "Sandstorm", "Thunderstorm", "Ancient Inferno Island", "Midnight Shadow Island", "Mythical Souls Island", "Winter Wonder Island"}
                for i = 1,#oh2 do
                    player.ninjaEvent:FireServer(oh1, oh2[i])
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.5) do
        if SkillEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local oh1 = "buyAllSkills"
                local oh2 = {"Ground", "Astral Island", "Space Island","Tundra Island", "Eternal Island", "Sandstorm", "Thunderstorm", "Ancient Inferno Island", "Midnight Shadow Island", "Mythical Souls Island", "Winter Wonder Island"}
                for i = 1,#oh2 do
                    player.ninjaEvent:FireServer(oh1, oh2[i])
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.5) do
        if RankEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local oh1 = "buyRank"
                local oh2 = ReplicatedStorage.Ranks.Ground:GetChildren()
                for i = 1,#oh2 do
                    player.ninjaEvent:FireServer(oh1, oh2[i].Name)
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.5) do
        if ShurikensEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local oh1 = "buyAllShurikens"
                local oh2 = {"Ground", "Astral Island", "Space Island","Tundra Island", "Eternal Island", "Sandstorm", "Thunderstorm", "Ancient Inferno Island", "Midnight Shadow Island", "Mythical Souls Island", "Winter Wonder Island"}
                for i = 1,#oh2 do
                    player.ninjaEvent:FireServer(oh1, oh2[i])
                end
            end
        end
    end
end)

spawn(function()
    while wait(0.033) do 
        if player.Character:WaitForChild("HumanoidRootPart") then
            if ChiEnabled then
                for i,v in pairs(Workspace.spawnedCoins.Valley:GetChildren()) do
                    if v.Name == "Blue Chi Crate" then 
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(v.Position)
                        wait(.16)
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(3) do
        if EvolveEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder:GetChildren()) do
                    for i,x in pairs(v:GetChildren()) do
                        local oh1 = "evolvePet"
                        local oh2 = x.Name
                        ReplicatedStorage.rEvents.petEvolveEvent:FireServer(oh1, oh2)
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(3) do
        if EternaliseEnabled then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder:GetChildren()) do
                    for i,x in pairs(v:GetChildren()) do
                        local oh1 = "eternalizePet"
                        local oh2 = x.Name
                        ReplicatedStorage.rEvents.petEternalizeEvent:FireServer(oh1, oh2)
                    end
                end
            end
        end
    end
end)

spawn(function()
    while wait(3) do
        if ImmortalizePet then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder:GetChildren()) do
                    for i,x in pairs(v:GetChildren()) do
                        local oh1 = "immortalizePet"
                        local oh2 = x.Name
                        ReplicatedStorage.rEvents.petImmortalizeEvent:FireServer(oh1, oh2)
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SBasic then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Basic:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SAdvanced then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Advanced:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SRare then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Rare:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SEpic then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Epic:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SUnique then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Unique:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SOmega then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Omega:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SElite then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Elite:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if SInfinity then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Infinity:GetChildren()) do
                    ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if S1 then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Infinity:GetChildren()) do
                    if v.Name == "Winter Wonder Kitty" then
                        ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if S2 then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Infinity:GetChildren()) do
                    if v.Name == "Winter Legends Polar Bear" then
                        ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if S3 then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Infinity:GetChildren()) do
                    if v.Name == "Christmas Sensei Reindeer" then
                        ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if S4 then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Infinity:GetChildren()) do
                    if v.Name == "Dark Blizzard Master Penguin" then
                        ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(1) do 
        if S5 then
            if player.Character:WaitForChild("HumanoidRootPart") then
                for i,v in pairs(player.petsFolder.Infinity:GetChildren()) do
                    if v.Name == "Cybernetic Sleigh Rider" then
                        ReplicatedStorage.rEvents.sellPetEvent:FireServer("sellPet", v)
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(.001) do 
        if FastShuriken then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local Mouse = player:GetMouse()
                local velocity = 1000
                for _,p in pairs(Workspace.shurikensFolder:GetChildren()) do
                    if p.Name == "Handle" then
                        if p:FindFirstChild("BodyVelocity") then
                            local bv = p:FindFirstChildOfClass("BodyVelocity")
                            bv.MaxForce = Vector3.new(math.huge,math.huge,math.huge)
                            bv.Velocity = Mouse.Hit.lookVector * velocity
                        end
                    end
                end
            end
        end
    end
end)

spawn(function() 
    while wait(.001) do 
        if SlowShuriken then
            if player.Character:WaitForChild("HumanoidRootPart") then
                local Mouse = player:GetMouse()
                local velocity = 35
                for _,p in pairs(Workspace.shurikensFolder:GetChildren()) do
                    if p.Name == "Handle" then
                        if p:FindFirstChild("BodyVelocity") then
                            local bv = p:FindFirstChildOfClass("BodyVelocity")
                            bv.MaxForce = Vector3.new(math.huge,math.huge,math.huge)
                            bv.Velocity = Mouse.Hit.lookVector * velocity
                        end
                    end
                end
            end
        end
    end
end)

local vu = game:GetService("VirtualUser")
player.Idled:connect(function()
    vu:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    wait(1)
    vu:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
end)

if not isMobile then
    Window:EditOpenButton({
        Title = "Keybind: K",
        Icon = "grip",
        CornerRadius = UDim.new(0,16),
        StrokeThickness = 2,
        OnlyMobile = false,
        Enabled = true,
        Draggable = false,
    })
    
    Window:SetToggleKey(Enum.KeyCode.K)
else
    Window:EditOpenButton({
        Title = "Ninja Legends",
        Icon = "grip",
        CornerRadius = UDim.new(0,16),
        StrokeThickness = 2,
        OnlyMobile = true,
        Enabled = true,
        Draggable = true,
    })
end
